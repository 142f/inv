# 代码审查与性能优化报告

## 1. 执行摘要

本次工作按“基线 Review → 第一轮优化 → 第二轮优化”完成三次正式审查。修改集中在已由测试或 Benchmark 证明的热路径和缺陷，未改变实盘许可、风控阈值、下单顺序或默认纸面执行机制。

- 测试从 31 项增加到 40 项，另含 4 个策略子测试，最终连续两次全部通过。
- 4000 根 K 线的动量回测从约 542.9 ms 降至 102–109 ms，约提升 5 倍。
- 8000 根 K 线的动量回测中位数为 207–213 ms，峰值追踪内存约 1.12 MiB。
- 四种研究策略中的 ATR、时间戳和 HOLD 信号实现已合并为一份；相关策略代码合计从约 785 行降至 677 行。

## 2. 架构、功能与数据流

### `core/` 实盘与保守研究路径

`run.py` 初始化 MT5 Broker、配置仓库、策略管理器和 `Runner`。`Runner` 每周期批量获取账户、挂单、持仓和按品种去重的 Tick，组装策略上下文，经风控协调后进入统一执行服务。`core/strategy/grid_strategy/` 承载实盘网格状态、目标单、对冲和自适应逻辑。`core/research/` 提供历史数据适配、质量校验、保守价格路径回测、成本压力测试和报告。

核心数据流：配置 → 类型化策略设置 → 市场快照 → 策略决策 → 风控决策 → 幂等执行命令 → Broker。

### `inv/` 标准化研究路径

`inv/domain/` 定义 K 线、信号、成交、绩效、合约规格和 Pipeline 输入输出。`inv/strategy/` 实现动量、均值回归、突破和网格策略。`inv/backtest/` 按严格时间顺序在下一根 K 线开盘执行信号，并提供参数搜索与 Walk-Forward。`inv/pipeline/` 组织数据校验、信号分析、回测、评估和报告阶段。

核心数据流：`Bar` 序列 → 当前已闭合窗口 → `Signal` → 下一期执行 → 持仓/现金/权益 → `PerformanceMetrics` → 报告。

`core/` 和 `inv/` 仍同时有效，不是可直接删除的新旧版关系。

## 3. 问题、严重程度与处理结果

| 级别 | 问题 | 结果 |
|---|---|---|
| 高 | 策略每根 K 线重建全历史价格数组，回测累计接近 `O(n²)` | 改为只读取算法所需的有界窗口，固定回看期下为 `O(n)` |
| 高 | 主动平仓后策略状态未清空，后续信号可能被误抑制 | 每个非 HOLD 信号执行后同步仓位、入场价和最后信号，已增加状态机回归测试 |
| 高 | `WalkForwardOptimizer(step_size=0)` 会产生无穷循环 | 初始化时拒绝非正窗口或步长 |
| 中 | 参数优化器未提供 `settings_factory` 时调用无效的无参 `StrategySettings()` | 从策略品种构造有效默认设置，外部接口不变 |
| 中 | 网格回测的每条目标命令都重复聚合持仓和挂单名义金额 | 从 `O(C·(P+Q))` 降为 `O(P+Q+C)`，并修复生成器输入被首次遍历耗尽的问题 |
| 中 | 四种策略重复 ATR、时间戳和 HOLD 信号逻辑 | 合并到现有 `BaseStrategy`，不新增抽象层 |
| 低 | 网格策略复制全部收盘价只为读取最后一个值 | 改为直接读取当前 K 线 |
| 低 | 空 Pipeline 在返回时引用未定义的局部变量 | 提前拒绝空阶段列表并返回明确中文错误 |
| 低 | 未使用的动量缓存、空 `reset_state` 覆盖和无用导入 | 已删除 |

## 4. 两轮优化记录

### 第一轮

1. 提取策略公共计算，消除四份重复实现。
2. 动量策略仅构造最多 `2 * lookback + 1` 个收盘价；均值回归仅构造 Bollinger 窗口；突破策略仅保留通道与确认窗口。
3. 网格回测命令协调改为名义金额增量统计。
4. 修复优化器默认配置、Walk-Forward 非正步长和空 Pipeline 边界。

### 第二轮

1. `cProfile` 确认剩余时间主要来自固定窗口内的 NumPy 标准差、数组构造和一次性绩效计算，已不再存在全历史重复扫描。
2. 修复回测引擎主动平仓后的状态不一致。
3. 增加运行时 IO 调用次数测试，保证每周期挂单和持仓各查询一次，Tick 按唯一品种查询。
4. 未引入滚动状态缓存、异步、多进程或实盘执行链改造，避免为小额外收益破坏可读性和确定性。

## 5. Benchmark 与测试

### 动量回测耗时

| K 线数 | 原始基线 | 第一轮中位数（两次） | 最终中位数（两次） |
|---:|---:|---:|---:|
| 500 | 23.5 ms | 12.5 / 13.7 ms | 13.6 / 13.3 ms |
| 1000 | 53.1 ms | 26.1 / 26.4 ms | 26.3 / 26.8 ms |
| 2000 | 157.2 ms | 50.4 / 50.8 ms | 52.2 / 52.6 ms |
| 4000 | 542.9 ms | 102.8 / 102.4 ms | 104.8 / 108.5 ms |
| 8000 | 未记录 | 208.1 / 207.2 ms | 210.4 / 212.8 ms |

最终 8000 根 K 线的其他策略中位数：均值回归 243.9–247.2 ms，突破 246.1–248.3 ms，网格 67.7–68.1 ms。两次 `tracemalloc` 记录的动量回测峰值均约为 1142 KiB。

测试最终结果：

- 第一轮：`37 passed, 4 subtests passed`。
- 第二轮首次：`38 passed, 4 subtests passed`。
- 最终两次：`40 passed, 4 subtests passed`，耗时分别约 0.25 s 和 0.26 s。

## 6. Review 评分

| 维度（10 分制） | Review 0 | Review 1 | Review 2 |
|---|---:|---:|---:|
| 正确性 | 7.0 | 8.6 | 9.3 |
| 兼容性 | 8.0 | 9.1 | 9.3 |
| 结构清晰度 | 6.3 | 7.8 | 8.3 |
| 重复控制 | 5.5 | 8.3 | 8.6 |
| 时间性能 | 5.0 | 8.9 | 9.0 |
| 内存与 IO | 7.0 | 8.5 | 8.8 |
| 测试覆盖 | 6.0 | 8.2 | 9.0 |
| 可维护性 | 6.2 | 8.1 | 8.7 |
| **平均** | **6.4** | **8.4** | **8.9** |

每个阶段均执行了两次检查：修改前的语义/复杂度检查，以及修改后的回归/性能检查。Review 2 后的剩余热点需要引入跨调用滚动缓存，其状态失效和随机索引兼容成本已高于可预期收益，因此停止继续扩张修改。

## 7. 保留问题与后续建议

1. `core` 与 `inv` 的领域模型、交易模型和绩效指标仍有重复；两套 `PerformanceMetrics` 字段不同，`inv` 额外包含 Calmar，直接合并会改变公开类型，本次不强行统一。
2. Pipeline 的信号分析阶段和回测阶段会分别执行策略。两者的状态语义不等价，直接复用信号会改变仓位反馈行为，因此本次保留。
3. `core/strategy/grid_strategy/` 仍有较大的 Mixin 和高复杂度方法。这些代码直接关系实盘下单，应先建立更完整的决策快照和模拟 Broker 端到端测试，再做职责拆分。
4. 参数优化仍为串行执行。只有在实际参数空间证明 CPU 成为主要瓶颈后，才建议增加显式、可选且确定性的多进程模式。
5. 本次未读取真实交易数据，Benchmark 使用固定公式生成的内存 K 线。如需评估真实负载，应由用户另行提供允许读取的脱敏样本。
6. 环境未安装 `pyflakes`，因此未执行该额外静态检查；现有测试已覆盖本次所有修改路径。
